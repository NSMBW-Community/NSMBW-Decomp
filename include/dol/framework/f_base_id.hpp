#pragma once
/// @file

/**
 * @brief A unique identifier for an fBase_c object.
 * @details [It's not quite clear why this was declared as an enum.
 * Ninji suggested it might be for type checking].
 * @ingroup framework
 */
enum fBaseID_e {
    FIRST_ID = 1,
    INVALID = -1
};
